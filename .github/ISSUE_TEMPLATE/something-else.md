---
name: Something else
about: A crash, a wrong number, a step that would not run
---

**What you did,** and what happened instead.

**Which version** (`git describe --tags`, or the app's About box) and which
macOS.

**The log,** if there is one: the last page of
`~/Library/Application Support/First Edit/studio.log`, which is what the
engine behind the app writes.
