**What is now true** that was not before, in a sentence.

**What it was measured against.** A change to what the cull shows or hides is
measured on every keeper of every shoot with verdicts, and the counts go here;
anything else, say which test covers it.

- [ ] `.venv/bin/python -m pytest -q tests` and `.venv/bin/ruff check pipeline app tests` pass
